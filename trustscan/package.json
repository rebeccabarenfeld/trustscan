{
  "name": "trustscan",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.0.0"
  }
}
```

→ **Commit changes**

---

### Fichier 2 — Modifie les settings Vercel

Dans Vercel → ton projet → **Settings → General** :
```
Build Command :
npm install && npm run build

Output Directory :
dist

Node.js Version :
20.x
```

→ **Save** → **Redeploy**

---

## Alternative encore plus simple

Dans Vercel → Settings → General → **Build Command** :
```
npm install && npx vite build
